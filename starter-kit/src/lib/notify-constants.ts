/** Stable clinic user id for `Notification.userId` (must not be random per boot). */
export const CLINIC_USER_ID = "11111111-1111-4111-8111-111111111111";

/** Default `Scan.patientId` when notify body omits `patientId` (no auth in this demo). */
export const DEFAULT_PATIENT_ID = "22222222-2222-4222-8222-222222222222";
